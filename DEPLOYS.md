# Deploys

Every deploy to `Releases\` gets a line here — version, date, source commit, folder.
Law: after copying the bundle, verify the marker string in `data\app.so` (a string
unique to this build's newest feature), the running process path, and the HKCU Run
registry value. An unrecorded deploy caused a full bug round (round 8: user tested
a 3-day-stale binary).

Law (public builds, added 2026-07-20): any build that leaves this machine is made
with `flutter build windows --release --obfuscate --split-debug-info=symbols\v<ver>`
and the `symbols\v<ver>` folder is KEPT (git-ignored, never deleted) — without it
tester crash logs are unreadable. The Rust DLL is already stripped (`strip=true`).
Local/dev deploys may skip obfuscation; the DEPLOYS line must then say so.

Installer (added 2026-07-21): testers get `Releases\Slate_Setup_v<ver>.exe`, built by
`tools\build_installer.ps1` (Rust + obfuscated release + app-local VC++ runtime +
Inno Setup `installer\slate.iss`). Per-user (`%LocalAppData%\Kael\Slate`), no admin,
publisher **Kael**, fixed AppId `{1F4B13C7-…}` for in-place upgrades. NOT code-signed
(no cert — SmartScreen wall stands; wave-0 uses "More info → Run anyway"). The
installer taskkills `slate.exe` before install/uninstall (window-close only hides to
tray), and uninstall clears the HKCU\Run `Slate` value while leaving user data.

Retention (added 2026-08-03): `Releases\` keeps the CURRENT and the PREVIOUS version
only — two `slate_v<ver>` bundles and their two `Slate_Setup_v<ver>.exe`, plus the
unversioned `Slate_Setup.exe`. That stable name is never deleted and is refreshed to
the newest build on every release: the landing page's download URL points at it
(`site\index.html`). Older bundles were 37 MB each and pure archive — the folder had
reached 571 MB. `symbols\v*` is exempt: every one is kept forever.

`tools\build_installer.ps1` now copies the bundle to `Releases\slate_v<ver>` itself
(step 5/6). It used to be a manual step, which is why v1.1.4 has no folder at all.

| Installer | Date | Commit | Artifact | Verified |
|-----------|------|--------|----------|----------|
| 1.1.8 | 2026-08-14 | (the reminder card: latency, entrance, dismiss) | Releases\Slate_Setup_v1.1.8.exe + Slate_Setup.exe (13.93 MB, same hash `712CBE49767885FDFF6BE2B2D96B`) + Releases\slate_v1.1.8 | **OBFUSCATED**, `symbols\v1.1.8` KEPT. app.so markers: `1.1.8` FOUND / `1.1.7` **ABSENT** (the negative marker proves the new code shipped) / `revealed` + `onReveal` + `ready` FOUND (the three new channel strings — the entrance and chime rework is genuinely in the bundle). exe VersionInfo `1.1.8+17`, company Kael. DLL 3-way hash match (root/target/bundle). RCDATA verified by `FindResource` without launching: 203 = 26532 B (done), 204 = 23892 B (reminder). analyze 0 errors / 0 warnings (306 pre-existing withOpacity infos); tests **208/208**. NOT code-signed. **NOT INSTALLED on this machine — the user runs the setup himself.** **ACCEPTANCE PENDING (audible + feel).** — **Caught by this table's own marker check:** the first cut had `1.1.8` ABSENT and `1.1.7` FOUND in app.so while the exe already read `1.1.8+17`. `lib/core/app_version.dart` carries the version as a Dart literal and takes it from nowhere; the exe takes its own from pubspec at build time, so the two can and did disagree. Rebuilt. This is exactly the failure the negative marker exists to catch. — **Contents.** The reminder card, measured rather than argued. (1) **Ask-to-pixel 462 ms → ~112 ms.** The uncloak was gated on `SchedulerBinding.addTimingsCallback`, which is a batched PROFILING api — the engine flushes FrameTiming records periodically, so the gate almost always hit its own 400 ms timeout. Replaced with a single `endOfFrame`; the "do not reveal an unpainted window" guarantee no longer rests on it, because a 1x1 `SetWindowRgn` held until Dart answers and a claimed `WM_ERASEBKGND` make it impossible to composite anything at all. (2) **The entrance used to play under the cloak** — `_reveal` started the spring on its first line and then waited, so what reached the screen was whatever was left of it, and on the first card of a session nothing was. The spring now starts on the same breath as the reveal answer. (3) **First card == every other card.** Shader compilation moved to a startup warm-up, triggered by a new `ready` call from Dart (it had been fired from `OnCreate`, before Dart installed its handler, and was silently dropped). The warm-up rehearses the WHOLE show — the first shell COM call, the first positioning `SetWindowPos`, the first `HWND_TOPMOST`, the first cloak transition — and four points of the entrance spring, not just the settled card. Measured entrance duration: 167 / 184 / 188 / 194 ms across cards 1-3 over two cold sessions, against 150 ms-to-half-width for card 1 versus 76 for card 3 before. (4) **The chime is no longer early.** It played when `ShowCards` returned, ~110 ms ahead of its own picture; `SfxPlayer::ArmDeferred`/`FireDeferred` now hold it until `Uncloak()`. (5) **Dismiss button**, top-left, straddling the corner, hover-only, 18 px — the done ring's own diameter, because they are the same kind of thing. macOS's placement, and the only free corner since the ring owns the right. Needed a new unclipped `overlay` slot on the shell. **It took THREE defects to make it actually work, and only clicking it on the real screen found them.** (a) `clipBehavior: Clip.none` lets a Stack PAINT outside its bounds but never HIT-TEST there — `RenderBox.hitTest` checks `size.contains` before looking at any child — so the overhanging half was drawn and completely dead. The shell now reserves `notifyDismissReach` (14 px) past the card on the left, right and top and insets the card back inside it; the card does not move a pixel, because the reserve is symmetric horizontally and absent at the bottom edge the scene pins. (b) `Listener` defaults to `deferToChild` and the only thing beneath it is a `CustomPaint`, whose `hitTest` refuses everything unless the painter opts in — so pointer events reached the button almost nowhere. It is `HitTestBehavior.opaque` now. (c) Leaving the card and arriving on the button are separate mouse-tracker updates, and in the gap neither hover flag was set: the button unmounted, and the enter it was about to receive had nothing to arrive at. It vanished from under the pointer reaching for it. A zero-duration bridge now holds the hover across the hand-off. Verified by clicking four points on the live card — the disc's centre, the crescent OUTSIDE the card, the disc's inner edge, and a corner of the target off the disc — each dismissing, and by photographing the button under its own pointer. Six widget tests pin all of it. (6) **The body is one opaque colour and the light has a direction.** The card was a tint — 0.72 over a DWM acrylic backdrop on Win11, 0.93 over nothing on Win10 — so it changed character with whatever was behind it, and the backdrop covering the whole extended frame is where the grey slab and the grey drag brackets came from in the first place. It is `notifyBody` now, opaque, three levels up from `background`, on every version; the compositor backdrop and the frame extension are gone with it, along with the `acrylic` flag through the whole chain. What makes it glossy is light, not transparency: a sheen down the body and a lit rim, both on ONE diagonal from top-left to bottom-right. That direction is the entire 3-D illusion — an intermediate cut brightened the rim evenly all round (0.20 hairline, 0.66 top) and it stopped being a lit edge and became a BORDER, which is flat; reverted to 0.14/0.50 and made directional instead. Measured along the top edge left to right: 92 → 76 → 59 → 51 → 45 → 39. (7) **Pointer-tracking light removed for good** (both the surface pool and the rim lens) on the user's call; the rim still brightens on hover, uniformly. (8) Answered exit 720 → 575 ms — the 140 ms dead pause after the tick is gone and the collapse overlaps it. (9) The card's shadow no longer ends in a hard line at the window region: blur 54 → 46 and padding 40 → 52 puts the boundary at 1.92σ, alpha 4/255. Verified on the live desktop with pixel probes (region boundary invisible: 30,30,30 either side; no bracket artefacts across a 14-step slow drag). |
| 1.1.7 | 2026-08-06 | (the completion chime) | Releases\Slate_Setup_v1.1.7.exe + Slate_Setup.exe (13.8 MB, same hash) + Releases\slate_v1.1.7 | **OBFUSCATED**, `symbols\v1.1.7` KEPT. app.so markers: `1.1.7` FOUND / `1.1.6` **ABSENT** (the negative marker proves the new code shipped) / `slate/sfx` FOUND. exe VersionInfo `1.1.7+16`, company Kael. DLL 3-way hash match (root/target/bundle). analyze 0 errors / 0 warnings (306 pre-existing withOpacity infos); tests **135/135** (4 new). **NOT INSTALLED on this machine yet** — the user runs the setup himself; the fixed AppId upgrades 1.1.6 in place (same `%LocalAppData%\Kael\Slate`, one Add/Remove entry, HKCU Run path unchanged, `slate_data` untouched), so there is no registry change to verify until he does. NOT code-signed. **AUDIBLE ACCEPTANCE PENDING.** Contents — Slate's first sound, and only one: a task struck through rings (`notification.wav`). Nothing else in the app makes noise. Architecture: a single **XAudio2 mixer in the runner** (`windows/runner/sfx_player.cpp`), NOT a pub.dev package — this process hosts TWO Flutter engines (main window + capture pill), and a Dart plugin would have to be registered on both, giving two device sessions with a seam between them. The clip is **RCDATA in the exe** (`Runner.rc`, id 203) — no file I/O, no path resolution, no way to ship a build with the audio missing; verified post-build by `FindResource` byte-for-byte (26532), because a wrong `.rc` path fails QUIETLY and yields a perfectly good but mute exe. WAV parsing walks the RIFF chunk list rather than assuming the canonical 44-byte header (a LIST chunk would otherwise be played as audio). Level 0.385, set against the clip's measured loudness (RMS −17 dBFS at a −5 dBFS peak — dense for its length, so unity would sit louder than anything else on the desktop); this lands it near −25 dBFS. The chime fires in **one direction only** (`toggled.isCompleted && !_undoing`) at the single funnel every checkbox goes through — un-ticking is not an achievement and an undo is the user taking the moment back. No settings, no tuning file: the value is compiled in. **Known gap: there is no way to turn the sound off** — fine for the author, a real question before wave 1. **Scope note:** an earlier cut of this release also had a pill-entrance sound and a drop click; both were removed before shipping on the user's call, and every file that served only them was reverted to its 1.1.6 bytes (`pill_window.dart`, `smart_day_input.dart`, `pulse_layer.dart`, `windows/runner/pill_window.{h,cpp}` — verified by an empty `git diff` against b670971). Net diff vs 1.1.6 is **two call sites** — `Sfx.init()` in `main.dart` and `Sfx.taskDone()` in `TaskState.toggleTask` — plus `lib/core/sfx/sfx.dart`, `windows/runner/sfx_player.{h,cpp}` and one WAV. One real bug was caught by the suite during that cut and is fixed: the fire-and-forget `invokeMethod` left a **rejectable future** behind, so on any host without the channel (`flutter test`, and any future embedder) it completed with `MissingPluginException` as an unhandled async error long after the tick — it now `.ignore()`s. A sound that cannot play is not an error worth reporting. |
| 1.1.6 | 2026-08-03 | (timeline anchor + Anytime rail + one lane model; REBUILT same evening with the pill screen-lift fix) | Releases\Slate_Setup_v1.1.6.exe + Slate_Setup.exe (13.7 MB, same hash) + Releases\slate_v1.1.6 | **REBUILT IN PLACE 2026-08-03 18:31 — the version was deliberately NOT bumped (user's call: replace 1.1.6, do not mint 1.1.7). Both setup exes and the bundle were overwritten, and this one IS INSTALLED (`%LocalAppData%\Kael\Slate`, 1.1.6+15, HKCU Run unchanged).** Adds one fix: the ~1-in-10 screen lift when the capture pill is summoned. Root cause MEASURED, not guessed — `tools\e2e\pill_flash.ps1` samples a full-width strip every 2 ms and caught the frame: the whole work area rises by exactly **+21,+17,+13 = 0x15110D**, i.e. `AppTheme.background`, the window-class brush from `win32_window.cpp`. On show, DefWindowProc erases the pill's client area with that brush; GDI writes no alpha, so DWM ADDS the graphite to the desktop under a per-pixel transparent window. Dropping the brush only trades graphite for a white fresh surface, so the lever is time: `ShowPill` now sets `DWMWA_CLOAK` before anything else and clears it only when Dart replies to `reveal`, which it does after two frames — pixels exist before the window is ever composited. `SetTimer` 150 ms belt: no reply → show anyway, never worse than before. The heal path (`ShowAtHealedSize`/`StepHeal`) runs under the same cloak. PROOF: control on the shipped 1.1.5 was **8 flashes / 20 summons** with 3 s idle before each; the same run on this build is **0 / 20**, twice. Plus a deterministic invariant test, `tools\e2e\pill_cloak_gate.ps1` — 8/8 summons cloaked-then-uncloaked with 0.0 ms ever visible-and-naked; it FAILS 6/6 on 1.1.5, which is what makes it a test. No latency cost: hotkey→pill on screen 114 ms avg / 134 ms worst, against 122/199 on 1.1.5. Regression: pill matrix **7/7**, geometry suite **G1–G4 PASS** with frames eyeballed, `pill_pixels` PASS + frame eyeballed, analyze 0/0/306, tests **131/131**. NOTE on markers: same version literal as the first 1.1.6 build, so a string grep CANNOT tell them apart — the functional marker is `pill_cloak_gate.ps1`, which only the new binary can pass. Original 1.1.6 contents below. |
| ~~1.1.6 (first build)~~ | 2026-08-03 | (superseded in place, see above) | — | Built by `tools\build_installer.ps1`. **NOT installed on this machine** — the user's live Slate (PID 5248, `%LocalAppData%\Kael\Slate`) was left running; installing would taskkill it. Verified from artifacts: app.so marker `1.1.6` FOUND / `1.1.5` **ABSENT**; exe VersionInfo `1.1.6+15`, company Kael; DLL 3-way hash match (root/target/bundle) `393A2D7F…`; obfuscated, `symbols\v1.1.6` KEPT. analyze 0 errors / 0 warnings (306 pre-existing withOpacity infos); tests **131/131** (was 118 — 13 new). NOTE on markers: this release adds no new user-facing string, and identifier names (`laneForDrop`, `snapExtent`) do NOT survive `--obfuscate`, so the version literal is the only usable positive marker — as with the 1.1.4 DLL note, the tests are the proof. Contents — three live-use bugs, each traced to one root cause and each covered by a test that was CONFIRMED to fail before the fix: (1) **the ribbon opened differently every time and sliced the hour label at the left edge** — the open offset centred a *minute* (now / the middle of the densest task window / noon) so it landed mid-column, and the wheel did `jumpTo(offset + rawDelta)`, so the bad sub-column phase survived every scroll and could not be dialled back; there were also TWO policies (entering a day vs the DAY button). Now one rule, always a whole column: today → current hour one column in; a day with tasks → earliest scheduled hour minus an hour of lead-in; empty day → 08:00. Wheel ticks whole hours through `DesktopScrollWrapper.snapExtent` (the same coalescing tick the week/month already use), and the drag edge-pan — the one thing allowed off-grid — eases back on session idle. (2) **the «Anytime» rail sat on the first card of an EMPTY day** (measured: 8.5px overlap): the rail hangs off a constant offset from the cell top ON PURPOSE, but week hid its progress ring and month its dots when the day was empty, so the head lost 17px / 7px and the list rose under a rail that had not moved. Both heads now reserve the slot; month's `settleTopOffset` corrected 34 → 36. It also kills the jolt where dropping the first task grew the head and shoved the list down a beat after the card landed. (3) **the drop ghost lay on top of an existing block and then the card fell a row** (measured: 41px = exactly one row stride): there were TWO lane packers — the block layer's sticky, `pref`-ordered `assignLanes`, and the ghost's `laneNearest`, which re-derived the rows from TaskState WITHOUT the prefs. A sticky layout and a fresh one can come out mirrored, so the ghost measured room on the wrong row. Now the block layer PUBLISHES its spans (`TimelineLayout`) and the ghost reads them; `laneNearest` is deleted; a drop asks for a row and never evicts one; the resolved row is reserved before the mutation so the layer reproduces it. Also `blockWidth()` — packing used the true duration while painting clamped to 48px, so sub-29-minute blocks shared a row and overlapped. NOT code-signed; VirusTotal pass still pending. **VISUAL ACCEPTANCE PENDING** |
| 1.1.5 | 2026-07-23 | (pill geometry heal + DPI + field reset) | Releases\Slate_Setup_v1.1.5.exe + Slate_Setup.exe (13.7 MB, same hash) | Built by `tools\build_installer.ps1`, **INSTALLED and verified live on this machine**: Add/Remove now reads 1.1.5, install location `%LocalAppData%\Kael\Slate`, autostart `--hidden` intact, app.so markers `1.1.5` FOUND / `1.1.4` ABSENT / `warmup` FOUND. Obfuscated, `symbols\v1.1.5` KEPT. analyze 0 errors / 0 warnings; tests 118/118 (2 new); pill matrix 7/7; geometry suite G1-G4 PASS with frames EYEBALLED at two different resolutions (1366x768 and, after Parsec reconnected, 2268x1080). Contents: (1) the boot-time stretched pill — see the 1.1.5 folder row below; (2) DPI — `Win32Window::Create` gained `physical_pixels`, because `main.cpp` passes the work area (already device pixels) and Create was scaling it again by dpi/96, so at 125% the pill window was born 1.25x the screen; (3) text typed but never submitted no longer survives a dismiss — `SmartInputNotifier.clear()` means "reset for a fresh capture" and the field now obeys it (it reset only the parse result before). NOT code-signed. Still to do before the UA wave: VirusTotal pass. |
| 1.1.4 | 2026-07-23 | (Ukrainian parser) | Releases\Slate_Setup_v1.1.4.exe + Slate_Setup.exe (13.7 MB) | **Ukrainian NLP added** before the UA tester wave — the parser knew only RU+EN, so «зустріч у пʼятницю о 18» parsed as nothing. Added: сьогодні/післязавтра, all UA weekdays (both apostrophe forms), «у» preposition, 12 UA months, «о»/«об» time prepositions + годині/години/година, днів/тижнів durations. `cargo test --release nlp` 37/37 incl. new `test_ukrainian`; no existing test broken. DLL 3-way hash match (root/target/bundle). NOTE: byte-marker check on the DLL is USELESS for these literal arrays — long-working RU strings («неделю», «недели») also fail to appear due to overlapping string-constant merging; only stems («тижд», «тижн») show. Tests are the proof, not the marker grep. Obfuscated, `symbols\v1.1.4` KEPT. NOT code-signed. Upload `Slate_Setup.exe` (stable name) to the public release so the landing's download URL keeps working. |
| 1.1.3 | 2026-07-21 | (this commit) | Releases\Slate_Setup_v1.1.3.exe (13.7 MB) | Built + contents verified from ISCC log (slate.exe, all plugin DLLs, msvcp140/vcruntime140/vcruntime140_1, slate_core.dll, data\ incl app.so+icudtl+flutter_assets). exe/setup metadata = Kael/Slate/1.1.3; new Kael icon embedded in exe (honey stroke). **LIVE install/launch/uninstall NOT run** — would taskkill the user's live Slate + drop a 2nd copy on his machine (he chose testers-only). Run on a clean account/VM, or after quitting the live tray Slate, to confirm end-to-end. NOT signed → VirusTotal + SmartScreen instruction still pending before wave 1. |

| Version | Date | Commit | Folder | Marker verified |
|---------|------|--------|--------|-----------------|
| 1.1.6 | 2026-08-03 (rebuilt 18:31) | (timeline anchor + Anytime rail + one lane model + pill screen-lift fix) | Releases\slate_v1.1.6 | **OBFUSCATED**, `symbols\v1.1.6` KEPT. app.so: `1.1.6` FOUND, `1.1.5` **ABSENT**. DLL 3-way hash match `393A2D7F…`. exe VersionInfo 1.1.6+15, company Kael. **INSTALLED and live** — `%LocalAppData%\Kael\Slate\slate.exe`, HKCU Run intact (`… --hidden`). Bundle + both setup exes overwritten in place; version deliberately not bumped. Verified ON THE INSTALLED ARTEFACT, not just the build folder: cloak gate 8/8, flash 0/20 (control 8/20), matrix 7/7, pill_pixels PASS + frame eyeballed. See the installer row above. Timeline/rail/lane work from the first 1.1.6 build still VISUAL ACCEPTANCE PENDING. |
| 1.0.0 … 1.1.3 | — | — | *(deleted 2026-08-03)* | Archive only, never the live binary — see the retention rule above. |
| 1.0.0 | 2026-07-09 | pre-git | Releases\slate_v1.0.0 | — |
| 1.0.1 | 2026-07-11 16:22 | pre-git (round 6; round 7 NOT included) | Releases\slate_v1.0.1 | — |
| 1.0.2 | 2026-07-14 10:55 | 9d3f2ac + round-8 WIP | Releases\slate_v1.0.2 | `slate_trace` + `Report a problem` in app.so; registry Run → v1.0.2; DLL hashes 3-way match. Superseded same day by 1.0.3 |
| 1.0.3 | 2026-07-14 15:04 | round-8 commits | Releases\slate_v1.0.3 | Matrix 12/12 — but the matrix only checked Win32 RECTS. The stretched pill was STILL broken: a Win32-correct fullscreen window can present a stale DWM surface. Superseded by 1.0.4 |
| 1.0.4 | 2026-07-14 22:xx | round-8 real-root fix | Releases\slate_v1.0.4 | Correct pill, but entrance animation ran off-screen (looked abrupt). Superseded by 1.0.5 |
| 1.0.5 | 2026-07-14 23:xx | round-8 pill + animation | Releases\slate_v1.0.5 | `1.0.5` in app.so; registry Run → v1.0.5; matrix 12/12 ×2 (pixel asserts). Cold morph's off-screen entrance run heals the swapchain; revealTick RESETS + replays the spring on-screen so the animation is smooth AND the pill is centered. Superseded by 1.0.6 (architecture change) |
| 1.0.6 | 2026-07-15 | separate pill window (18e1c06) | Releases\slate_v1.0.6 | `1.0.6` + `runPillWindow` in app.so; registry Run → v1.0.6; new pill-window matrix 7/7 ×2. ARCHITECTURE: the capture pill is now its own always-on-top window (own Flutter engine) — the main window is NEVER morphed, so the whole rounds 1-8 bug class is gone. +655/−1498 (morph Frankenstein deleted) |
| 1.0.6 (re-cut) | 2026-07-15 later | tray + maximize hotfixes | Releases\slate_v1.0.6 | Same version at user's request; two hotfixes on top: (1) pill window registers ONLY flutter_acrylic (its 2nd tray_manager was fighting the main one → tray icon went unclickable); (2) maximize button re-reads real Win32 state on onWindowResize + delayed recheck (minimize-from-max → restore desynced the icon). Verify by build timestamp, not version string |
| 1.0.7 | 2026-07-20 | audit-fix wave (5fd5d70, 3780a3b) + prefs-resurrection fix | Releases\slate_v1.0.7 | Markers `1.0.7` + `Gym at 6pm #health` (hints) + `can't open your tasks` (VaultGate) in app.so; DLL 3-way hash match (root/target/bundle); registry Run → v1.0.7 --hidden. NOT obfuscated (local deploy, per law note). Contents: EN parser (5pm/at 5/from-to/slang), VaultGate + typed init codes + capture spill, AppData home + Documents migration, daily backups ×7, teaching placeholders, honest hotkey fallback, WM_ENDSESSION flush, atomic prefs; legacy secure-storage prefs migration REMOVED (it resurrected onboarded/welcomed after a wipe — user saw no welcome). Visual acceptance of VaultGate/placeholders/welcome-fallback pending |
| 1.1.0 | 2026-07-20 | 0bf0a92 (runner/UI) + 6e9a319 (day pane) | Releases\slate_v1.1.0 | **MVP release build — OBFUSCATED**; `symbols\v1.1.0\app.windows-x64.symbols` KEPT (2.6 MB — without it tester crash logs are unreadable; never delete, and it only decodes THIS build). Markers in app.so: `ANYTIME` and `1.1.0` FOUND, `TO SCHEDULE` **ABSENT** — the negative marker is the proof the new code actually shipped. DLL 3-way hash match (root/target/bundle). Registry Run → v1.1.0 --hidden (was 1.0.7). analyze 0 errors / 0 warnings; tests 106/106. Contents: the day's left pane now speaks "show the future" (DropFuture) like week/month and is ONE Anytime destination — the keep/clear split and the grey drop-wash are deleted, taking with them the invisible 40% boundary on days with no untimed tasks; TO SCHEDULE → ANYTIME; struck-through hour hand-over on the group head. Also: white launch flash fixed (window-class brush + first frame off the raster thread + lazy PillWindow), month pixel-snapping, Esc no longer swallowed on week/month, zoom invite no longer seals the lesson ladder. NOT code-signed — the SmartScreen wall still stands for strangers: fine for wave 0 (hand-installed), a blocker for wave 1. VISUAL ACCEPTANCE PENDING |
| 1.1.1 | 2026-07-21 | 9bbf65b (month cap) + 5970809 (month open) + 9f117ca (dnd truth) | Releases\slate_v1.1.1 | **OBFUSCATED**, `symbols\v1.1.1` KEPT (2.6 MB, decodes only this build). Markers in app.so: `1.1.1` + `ANYTIME` FOUND. DLL root==bundle hash match. Registry Run → v1.1.1 --hidden (was 1.1.0). analyze 0/0; tests 112/112. Contents on top of 1.1.0: month «+N more» is now a real button — tapping opens the WHOLE day as a floating layer so a hidden task is reachable and draggable, and the label no longer clips off short cells (cap measured like the week, was hardcoded 2 → the user saw two tasks and no button); drop preview no longer lies about position (importance ordering, day/week/month) or flies to an empty spot when a card sorts below the cap (dissolves into the «+N more» pile). NOT code-signed. VISUAL ACCEPTANCE PENDING |
| 1.1.2 | 2026-07-21 | c67ebe2 (opened-day refinements) | Releases\slate_v1.1.2 | **OBFUSCATED**, `symbols\v1.1.2` KEPT (2.6 MB, decodes only this build). Markers in app.so: `1.1.2` FOUND (unique vs 1.1.1). DLL root==bundle hash match. Registry Run → v1.1.2 --hidden (was 1.1.1). analyze 0/0; tests 115/115. Contents on top of 1.1.1 (three fixes to the month opened-day popover): (1) timed/untimed split under SCHEDULED / ANYTIME labels (was one flat pile); (2) month paging FROZEN while a day is open — DesktopScrollWrapper gained a `paused` listenable checked before it acts on the wheel (it reads the wheel synchronously and skips the pointer resolver, so a descendant scrim/AbsorbPointer could not stop it; the shared flag is set by the opening page, read by the wrapper above) → the popover now owns the wheel and scrolling it no longer pages months; (3) «+N more» hovers — brighter label on a soft pill, cursor stays basic. NOT code-signed. VISUAL ACCEPTANCE PENDING |
| 1.1.5 | 2026-07-23 | (pill geometry heal) | Releases\slate_v1.1.5 + installed to %LocalAppData%\Kael\Slate | **OBFUSCATED**, `symbols\v1.1.5` KEPT (2.6 MB, decodes only this build). Markers in app.so: `1.1.5` FOUND, `1.1.4` **ABSENT** (the negative marker proves the new code shipped), `warmup` FOUND. DLL 3-way hash match (root/bundle/installed). Registry Run → installed path `--hidden` (it had been hijacked to the dev `build\` path while testing — restored by launching the installed exe). analyze 0 errors / 0 warnings (306 pre-existing withOpacity infos); tests 116/116; pill matrix 7/7; new geometry suite G1-G4 PASS + frames eyeballed. FIXES the bug the user hit on every boot: the pill spawned as a stretched slab in a corner and stayed broken for the whole session. Root cause: the pill window is sized once at startup and ShowPill resizes it just before showing — but **a resize is only adopted by an engine that is actively presenting**, and between summons this one draws nothing, so the viewport stayed stale (640x480 inside a 1366x768 window). Trigger on this machine: the monitor is broken, the desktop is streamed to a laptop via Parsec, and Parsec switches the host resolution AFTER autostart builds the pill. Fix: on a size mismatch, show → `warmup` (Dart ticks a fully transparent controller so frames flow) → nudge the size → only then play the entrance. Unchanged geometry keeps the old code path byte for byte. NOT code-signed. Installer for testers NOT rebuilt yet |
| 1.1.3 | 2026-07-21 | a2c236e (corner button + open/close anim) | Releases\slate_v1.1.3 | **OBFUSCATED**, `symbols\v1.1.3` KEPT (2.6 MB, decodes only this build). Marker in app.so: `1.1.3` FOUND. DLL root==bundle hash match. Registry Run → v1.1.3 --hidden (was 1.1.2). analyze 0/0; tests 116/116. Fixes a regression + polish on the month cell: (1) cells showed only ONE card after the cap-measuring change reserved a label row — «+N more» moved OUT of the vertical flow to a bottom-right CORNER button (dark pill, hover-brighten, still a SettleAnchor drop pile), card cap floors at two and grows on taller cells; (2) the opened-day popover now eases in AND out — one AnimationController drives scale+fade from the cell (forward easeOutCubic / reverse easeInCubic, scrim on the same curve, painted one beat past close so the fold-away plays), filterQuality guarded. NOT code-signed. VISUAL ACCEPTANCE PENDING |
